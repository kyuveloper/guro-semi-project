/*import connection from 'mysql/lib/Pool';

const query = 'SELECT user_name FROM tbl_all_users WHERE user_code = 1';

connection.query(query, (error, results) => {
    if (error) {
        console.error(error);
    } else {
        console.log(results);
    }
});*/
/*
import mysql from 'mysql';

const pool = mysql.createPool({
    host: "localhost",
    port: 3306,
    user: "menu",
    password: "menu",
    database: "opez"
});

const query = 'SELECT user_name FROM tbl_all_users WHERE user_code = 1';

pool.query(query, (error, results) => {
    if (error) {
        console.error(error);
    } else {
        console.log(results);
    }
});*/
